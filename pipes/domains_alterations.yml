name: domains_alterations
input:
  table: assets
  as_file: ${.input.hostname}
cmd: cat ${.input.as_file} | assets | cut -d' ' -f1
output:
  table: assets
  ident: ${.output}
  hostname: ${.output}
interval: 48h
worker: 1
